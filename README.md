# unnamed.compiler
A simple compiler written in rust [WIP]
## Running
```rust
cargo run <filename.un>
```
> The <filename.un> is a `unnamed` source file, my naive attempt at creating a custom programming language. An example can be found in the main.un file.
